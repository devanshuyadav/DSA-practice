#include <iostream>
using namespace std;

int main()
{
    // your code goes here
    int a, b, x, y;
    scanf("%d %d %d %d", &a, &b, &x, &y);
    int ans = (a * x) + (b * y);
    printf("%d", ans);
    return 0;
}
// AC